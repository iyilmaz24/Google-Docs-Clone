# your-project-root/BUILD
# This is the root BUILD file.
# It can be empty if all targets are defined in subdirectories.

# You could define project-wide aliases or filegroups here if needed.
# package_group(
#     name = "all_my_packages",
#     packages = ["//..."],
# )